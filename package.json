{
  "name": "hyper-match",
  "version": "1.0.0",
  "description": "Quick arcade fun about matching the right colour",
  "main": "src/index.ts",
  "scripts": {
    "build": "yarn clean && parcel build index.html --no-source-maps --public-url . --out-dir dist",
    "start": "yarn clean && parcel index.html",
    "clean": "rm -rfv dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/msanatan/hyper-match.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/msanatan/hyper-match/issues"
  },
  "homepage": "https://github.com/msanatan/hyper-match#readme",
  "dependencies": {
    "fonts": "0.0.2",
    "phaser": "^3.24.1"
  },
  "devDependencies": {
    "@types/css-font-loading-module": "0.0.4",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.5.0",
    "typescript": "^4.1.2"
  },
  "staticFiles": {
    "staticPath": "assets",
    "watcherGlob": "**"
  }
}
